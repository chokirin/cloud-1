- name: Create nginx config directory
  file:
    path: "{{ app_dir }}/nginx"
    state: directory
    mode: '0755'

- name: Deploy nginx.conf
  template:
    src: nginx.conf.j2
    dest: "{{ app_dir }}/nginx/nginx.conf"
    mode: '0644'